apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: todo-api-quarkus
  namespace: johannes-workshop
spec:
  to:
    kind: Service
    name: todo-api-quarkus
  port:
    targetPort: http
  tls:
    termination: Edge
  wildcardPolicy: None